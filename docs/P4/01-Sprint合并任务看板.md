# P4 Sprint 合并任务看板

更新时间：2026-06-06

本文档基于以下事实整理：

- 本地工作区检出自 `nju/p4`，与 GitHub `main` 同一提交：`b73e03b`
- 代码、测试、流水线配置均以当前仓库实际内容为准
- 状态分为 `已完成`、`已完成待环境复核`、`可选`

## 1. Sprint 目标

- 完成 P0 核心业务闭环：注册/登录、任务发布、接单、订单状态流转
- 完成 P1 基础能力：评价与信用、消息通知
- 保证前后端可构建、核心单元测试可运行、集成测试已接入 CI
- 形成可人工验收并可上传 GitLab 的 P4 文档包

## 2. 任务看板

| 优先级 | 模块 | 任务 | 估计工时 | 负责人角色 | 当前状态 | 完成标准 |
| --- | --- | --- | --- | --- | --- | --- |
| P0 | 用户管理 | 注册、登录、个人信息、信用页、实名认证提交 | 4h | 后端 + 前端 | 已完成待环境复核 | `AuthController`、`UserController`、登录/注册/个人中心页面齐备，后端单元测试通过 |
| P0 | 需求发布 | 发布任务、任务列表、详情、收藏、评论 | 4h | 后端 + 前端 | 已完成待环境复核 | `TaskController`、`TaskCommentController`、发布/列表/详情页面齐备 |
| P0 | 订单管理 | 接单、确认、完成、取消、拒绝、放弃、订单列表/详情 | 4h | 后端 + 前端 | 已完成待环境复核 | `OrderController`、订单前端页面齐备，状态流转服务和测试存在 |
| P0 | 基础设施 | 统一响应、异常处理、JWT、安全配置、数据库脚本、Redis/Redisson | 4h | 后端 | 已完成 | `campushub-common`、`schema.sql`、`application*.yml`、`docker-compose.yml` 已齐备 |
| P1 | 评价与信用体系 | 提交评价、按订单/用户查询评价、信用分计算与记录展示 | 3h | 后端 + 前端 | 已完成待环境复核 | `ReviewController`、`CreditCalculator`、`ProfileCredit.vue` 存在，测试通过 |
| P1 | 消息通知 | 站内消息、已读/全部已读、删除、公告、订单聊天 | 3h | 后端 + 前端 | 已完成待环境复核 | `MessageController`、`NoticeController`、`ChatController` 与消息中心页面存在 |
| P1 | 管理能力 | 用户封禁、实名审核、任务审核、举报处理、统计看板、公告管理 | 4h | 后端 + 前端 | 已完成待环境复核 | `/api/admin/**` 与管理端页面齐备 |
| 测试 | 单元测试 | 服务层、控制器层、契约层、并发边界 | 6h | 测试 + 后端 | 已完成 | 49 个非集成测试类，2026-06-06 本地执行 283 个测试全部通过 |
| 测试 | 集成测试与 CI | Testcontainers 集成测试、GitLab 子流水线、前后端构建脚本 | 4h | 测试 + DevOps | 已完成待环境复核 | 6 个集成测试类已接入 `.gitlab/backend.yml`；本地无 Docker 时自动跳过 |
| P2 | 可选扩展 | 论坛模块、更多推荐/统计能力 | 4h | 可选 | 可选 | 当前仅保留模块骨架，不阻塞 P0/P1 交付 |

## 3. 分工建议

如果你准备按课程要求提交真实团队分工，建议把上表中的“负责人角色”替换成实际成员姓名。当前文档先保留角色分工，便于人工复核时快速调整。

推荐映射方式：

- 需求/总控：负责 Sprint 裁剪、文档合并、验收清单
- 后端：负责 `backend/campushub-parent` 下业务模块与测试
- 前端：负责 `frontend/src` 页面、路由、API 封装
- 测试/CI：负责集成测试、流水线配置、演示前验收

## 4. 风险与处理

| 风险 | 影响 | 当前处理 |
| --- | --- | --- |
| 本机无 Docker | 集成测试无法真实拉起 PostgreSQL 容器 | 已验证：Testcontainers 会跳过测试；GitLab CI 通过 `docker:dind` 执行 |
| 当前仓库未接入 JaCoCo | 无法自动产出覆盖率百分比 | 已保留测试分布和执行结果；人工上传前可决定是否补 coverage 插件 |
| 演示依赖 PostgreSQL/Redis | 影响助教现场复现 | 已在 `docker-compose.yml` 和演示说明中给出启动方式 |
| 论坛模块仍为空壳 | 不影响 P0/P1，但不宜纳入主演示路径 | 明确标记为 P2 可选 |

## 5. 本阶段完成判定

截至 2026-06-06，可认定以下工作已经落地：

- 代码层面：P0/P1 所需的主要控制器、服务、页面、测试和流水线均已存在
- 构建层面：前端构建成功，后端单元测试成功，后端打包成功
- 文档层面：本次新增 `docs/P4/` 交付包，供人工检查后上传 GitLab

仍需人工最终确认的部分：

- 用本机或 GitLab Runner 真正拉起 PostgreSQL/Redis/Docker 后完成一次端到端演示
- 如老师强制要求“覆盖率百分比截图”，需补充 JaCoCo 或 IDE 覆盖率截图
