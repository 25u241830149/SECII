# 可运行系统代码说明

更新时间：2026-06-06

## 1. 代码基线

- GitHub 仓库：`25u241830149/SECII`
- GitLab 分支：`bug-fixed-correct/sec-ii-2026:p4`
- 当前本地检出提交：`b73e03b`
- 结论：GitHub `main` 与 GitLab `p4` 当前内容一致，可直接作为 P4 交付代码基线

## 2. 目录结构

```text
backend/campushub-parent/
frontend/
.gitlab-ci.yml
.gitlab/backend.yml
.gitlab/frontend.yml
docker-compose.yml
docs/
```

## 3. P0 功能落地情况

| P0 能力 | 后端证据 | 前端证据 | 当前判断 |
| --- | --- | --- | --- |
| 注册/登录 | `campushub-user/.../AuthController.java` | `frontend/src/views/auth/LoginView.vue`、`RegisterView.vue` | 已实现 |
| 个人资料/信用 | `UserController.java`、`CreditService.java` | `ProfileHome.vue`、`ProfileEdit.vue`、`ProfileCredit.vue` | 已实现 |
| 任务发布/列表/详情 | `TaskController.java`、`TaskQueryService.java` | `PublishTask.vue`、`Home.vue`、`TaskDetail.vue` | 已实现 |
| 任务评论/收藏 | `TaskCommentController.java`、`TaskFavoriteService.java` | `TaskCommentSection.vue`、详情页收藏交互 | 已实现 |
| 抢单/接单 | `OrderController.java`、`GrabService.java` | `OrderList.vue`、`OrderDetail.vue` | 已实现 |
| 订单状态流转 | `OrderStatusService.java` | `OrderStatusBadge.vue`、订单详情页动作按钮 | 已实现 |
| 统一异常/鉴权/基础配置 | `campushub-common` 模块 | 前端 `stores/auth.ts`、`api/request.ts` | 已实现 |

## 4. P1 功能落地情况

| P1 能力 | 后端证据 | 前端证据 | 当前判断 |
| --- | --- | --- | --- |
| 评价提交/查询 | `ReviewController.java`、`ReviewService.java` | `ReviewForm.vue`、`ReviewList.vue` | 已实现 |
| 信用分计算 | `CreditCalculator.java`、`CreditService.java` | `ProfileCredit.vue` | 已实现 |
| 站内消息 | `MessageController.java`、`MessageService.java` | `MessageCenter.vue` | 已实现 |
| 系统公告 | `NoticeController.java`、`NoticeService.java` | `admin/NoticeManagement.vue` | 已实现 |
| 订单聊天 | `ChatController.java`、`ChatService.java` | `ChatPanel.vue` | 已实现 |
| 举报/后台处理 | `ReportController.java`、`AdminStatsController.java` | `ReportCreate.vue`、`admin/ReportCenter.vue`、`admin/StatsDashboard.vue` | 已实现 |

## 5. 本地构建与打包记录

### 前端

执行时间：2026-06-06

```powershell
cd frontend
npm.cmd ci
npm.cmd run build
```

结果：

- `npm.cmd ci` 成功
- `npm.cmd run build` 成功
- 产物目录：`frontend/dist/`

### 后端

执行时间：2026-06-06

```powershell
cd backend/campushub-parent
mvn --% -Dtest=!*IntegrationTest -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false test
mvn -DskipTests package
```

结果：

- 非集成测试执行成功
- `package` 打包成功
- 可执行 Jar：`backend/campushub-parent/campushub-bootstrap/target/campushub-bootstrap-0.0.1-SNAPSHOT.jar`

## 6. 运行前置条件

### 必需依赖

- Java 17
- Maven 3.9+
- Node.js 22+
- PostgreSQL
- Redis

### 推荐启动基础环境

```powershell
docker compose up -d
```

如果本机没有 Docker，可改为手工启动 PostgreSQL 和 Redis，并保证：

- PostgreSQL 数据库名：`secii_db`
- Redis 端口：`6379`

## 7. 启动方式

### 启动后端

```powershell
cd backend/campushub-parent
mvn -pl campushub-bootstrap -am -DskipTests package
java -jar campushub-bootstrap/target/campushub-bootstrap-0.0.1-SNAPSHOT.jar
```

默认地址：

- 后端：`http://localhost:8080`
- Swagger：`http://localhost:8080/swagger-ui.html`

### 启动前端

```powershell
cd frontend
npm.cmd install
npm.cmd run dev -- --host 127.0.0.1
```

默认地址：

- 前端：`http://127.0.0.1:5173/`

## 8. 结论

从代码结构、控制器/页面覆盖范围、前端构建结果和后端打包结果看，仓库已经具备 P4 所要求的“可运行系统代码”基础。当前唯一未在这台机器上完成的，是带数据库和 Redis 的完整联调演示，这部分需要你在有 Docker 或现成中间件环境的机器上再做一次人工确认。
